---
---

<section class="manifesto" aria-label="Manifiesto">
  <div class="container">
    <blockquote class="text-manifesto text-inverse reveal">
      Cada organización tiene una historia que vale la pena contar.
      El desafío no es la historia&nbsp;—
      es encontrar las palabras exactas, el momento preciso
      y la valentía de decirlo bien.
      <br /><br />
      Eso es lo que hacemos juntos.
    </blockquote>
  </div>
</section>

<style>
  .manifesto {
    background-color: var(--color-bg-dark);
    padding-block: var(--space-120);
  }

  .text-manifesto {
    font-family: 'Cormorant Garamond', Georgia, serif;
    font-size: clamp(1.25rem, 2.5vw, 1.75rem);
    font-weight: 300;
    font-style: italic;
    line-height: 1.65;
    letter-spacing: -0.01em;
    text-align: center;
    max-width: 760px;
    margin-inline: auto;
    color: var(--color-text-inverse);
    word-spacing: 0.04em;
    quotes: none;
  }

  @media (max-width: 768px) {
    .manifesto {
      padding-block: var(--space-80);
    }
  }
</style>
